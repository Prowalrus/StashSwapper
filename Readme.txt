Make sure it's in the root SPT folder (e.g. <drive>/SPT)
Using PROFILE (not character) names, choose the old profile name followed by the new profile name.
It should run fine.
Any issues, hit up the thread!